- defaults:
    link:
      relink: true
      force: true
      create: true

- clean: ['~']

- link:
    ~/.config/kitty/kitty.conf:
      path: ../config/kitty/kitty.conf
    ~/.config/kitty/color.conf:
      path: ../config/kitty/color.conf

- shell:
  - [git submodule update --init --recursive, Installing submodules]
