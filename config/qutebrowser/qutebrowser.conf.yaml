- defaults:
    link:
      relink: true
      force: true
      create: true

- clean: ['~']

- link:
    ~/.config/qutebrowser/config.py:
      path: config.py

- shell:
  - [git submodule update --init --recursive, Installing submodules]
